[pytest]
DJANGO_SETTINGS_MODULE = config.settings
testpaths =
    tests
    guests/tests
python_files = test_*.py *_tests.py
addopts = --strict-markers
markers =
    e2e: high-value end-to-end workflow tests across views/services/models
    integration: integration tests that require external MySQL/Redis services
